###############################################################################
# Session Management
###############################################################################

## Reload WM Config
bindsym $alt+Shift+c reload

## Refresh Session
bindsym $alt+Shift+r exec --no-startup-id /usr/bin/regolith-look refresh

## Restart WM
set_from_resource $wm.binding.restart wm.binding.restart Ctrl+r
bindsym $alt+Ctrl+r restart

## Lock Screen
bindsym $alt+Shift+Escape exec dbus-send --type=method_call --dest=org.gnome.ScreenSaver /org/gnome/ScreenSaver org.gnome.ScreenSaver.Lock

## Sleep
bindsym $alt+Shift+s exec systemctl suspend
